# 2026-07-05 — Sunday early morning: MSC-IRE auto-study session

## ⏰ Session trigger

User asked at 02:04 UTC: 「你同以後我自動化簡第一選項 幫我自動學weekend bootcamp 然後 git push」

Two directives:
1. **Going forward, when running automation for the user, pick the first option by default — don't ping back asking which option.**
2. **Auto-run the weekend bootcamp (MSC-IRE), then git push.**

## 🎓 What I did

**Week 4 (2026-07-04 / 2026-07-05 weekend):** Phase 1 Foundations, course = MAEG3010 Mechanics of Materials.

Generated material via existing `bootcamp-content-assistant` skill:

- `scripts/flashcard_gen.py MAEG3010 --format anki` → 5 cards (saved to `skills/.../cards.tsv`, gitignored)
- `scripts/practice_gen.py MAEG3010 --count 6 --format json` → 6 explain-prompts (saved to `skills/.../outputs/MAEG3010_practice.json`, gitignored)
- `scripts/course_read.py MAEG3010 --summary` → used to write synthesized notes

**New committed artefacts:**

- `study/msc-ire/README.md` — week index + auto-study workflow doc
- `study/msc-ire/week-04/README.md` — week index entry
- `study/msc-ire/week-04/MAEG3010-notes.md` — synthesized theory, key formulas table, robotics relevance, simulator update hooks, study plan
- `study/msc-ire/week-04/practice.md` — 6 explain-prompts with answer slots

## 🪜 Boundary respected

- IRE-only — no MW01 / RSE / Minor Works content touched.
- Original bootcamp mirror at `/app/bootcamps/msc-ire/` is read-only — did not modify upstream.

## 📌 Preferences to keep in mind for future sessions

- Pick the first option by default; don't ask "which option?".
- IRE is the weekend bootcamp to auto-run; MW01 is NOT in scope during IRE sessions.
- After every weekend session: update notes in `study/msc-ire/week-NN/`, log here, `git add -A && git commit && git push`.
- If push fails (no auth), surface the failure to the user via Telegram, don't silently drop.

## ⏭ Next

- Week 5 (2026-07-11): EEEN3030 Materials + ELEG2202 Circuits — sensor modeling sim update.
- Pending: user fills in `practice.md` answers + actually does the Anki review.
